import { IProduct } from 'types';

type Props = {
  product: IProduct;
};

const ProductDetails = ({ product }: Props) => {
  return (
    <>
      {/* BOX */}
      {/* MEDIA OBJECT */}
      {/* PRODUCT FEATURES */}
    </>
  );
};

export { ProductDetails };
