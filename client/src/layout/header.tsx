import { GlobalContainer } from '../styles';

type Props = {};

const Header = (props: Props) => {
  return (
    <GlobalContainer as='header'>
      <h1>HEADER</h1>
    </GlobalContainer>
  );
};

export { Header };
