import { GlobalContainer } from '@src/styles';

type Props = {};

const Footer = (props: Props) => {
  return (
    <GlobalContainer as='footer'>
      <h1>HEADER</h1>
    </GlobalContainer>
  );
};

export { Footer };
