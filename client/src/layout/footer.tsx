import { GlobalContainer } from '../styles';
type Props = {};

const Footer = (props: Props) => {
  return (
    <GlobalContainer as='footer'>
      <h1>FOOTER</h1>
    </GlobalContainer>
  );
};

export { Footer };
