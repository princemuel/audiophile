import { GlobalContainer } from '../styles';

type Props = {};

const Footer = (props: Props) => {
  return (
    <footer>
      <GlobalContainer>
        <h1>HEADER</h1>
      </GlobalContainer>
    </footer>
  );
};

export { Footer };
