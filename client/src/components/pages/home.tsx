import { GlobalContainer } from '@src/styles';

type Props = {};

const Home = (props: Props) => {
  return (
    <GlobalContainer>
      <h1>Home</h1>
    </GlobalContainer>
  );
};

export { Home };
