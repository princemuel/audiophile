import { GlobalContainer } from '@src/components';

type Props = {};

const Home = (props: Props) => {
  return (
    <GlobalContainer>
      <h1>Home</h1>
    </GlobalContainer>
  );
};

export { Home };
